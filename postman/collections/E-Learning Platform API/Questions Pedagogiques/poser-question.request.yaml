$kind: http-request
name: 'Poser Question'
method: POST
url: '{{baseUrl}}/api/questions'
order: 1000
auth:
  type: bearer
  credentials:
    - key: token
      value: '{{token}}'
headers:
  - key: Content-Type
    value: application/json
body:
  type: json
  content: |-
    {
      "enonce": "Comment fonctionne @Transactional ?",
      "coursId": 2
    }
