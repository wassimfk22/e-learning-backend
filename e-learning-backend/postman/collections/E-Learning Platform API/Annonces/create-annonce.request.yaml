$kind: http-request
name: 'Create Annonce'
method: POST
url: '{{baseUrl}}/api/annonces'
order: 2000
auth:
  type: bearer
  credentials:
    - key: token
      value: '{{token}}'
headers:
  - key: Content-Type
    value: application/json
body:
  type: json
  content: |-
    {
      "titre": "Maintenance prévue",
      "contenu": "Le système sera indisponible ce weekend."
    }
